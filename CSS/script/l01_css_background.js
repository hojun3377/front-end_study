'use strict'

// selector
const testBox = document.getElementById("testBox");
const bgTestList = document.getElementById("bgTestList").children;

Array.from(bgTestList).forEach((el, i) => {
  el.value = {f
  el.onclick = () => {
    if(el.value) testBox
    else
  }
});
